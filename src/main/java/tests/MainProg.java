package tests;

import models.Booking_l;
import models.Category_l;
import models.Lieu;
import services.BookingService;
import services.CategoryService;
import services.LieuService;

import java.util.Date;
import java.util.List;

public class MainProg {
    public static void main(String[] args) {

/*
        CategoryService ps2 = new CategoryService();
        System.out.println(ps2.afficher());
        ps2.ajouter(new Category_l("Hotel"));

        //ps2.supprimer(new Category_l(6,"H"));
        System.out.println(ps2.afficher());

        // Call the modifier method
        //ps2.modifier(new Category_l(4,"villa"));
        //System.out.println(ps2.afficher());
        // Création d'une instance de LieuService
          LieuService ps = new LieuService();
         // System.out.println("Liste des lieux avant l'ajout :\n");
         // System.out.println(ps.afficher());

        // ps.ajouter(new Lieu(new Category_l(3,"Catégorie Test"),"teeest", 50.0, "test.jpg", "2024-04-01", "2024-04-15", 100, "Testville", 30.0, 40.0));
        // Tester l'affichage des lieux après l'ajout
       // System.out.println("Liste des lieux après l'ajout :\n");
       // System.out.println(ps.afficher());



        // Récupérez le lieu à modifier en utilisant son identifiant

        Lieu lieuAModifier = ps.obtenirLieuParId(1);

// Vérifiez si le lieu existe
        // if (lieuAModifier != null) {
            // Modifiez les propriétés du lieu selon vos besoins
            //lieuAModifier.setNom("rades");
            //lieuAModifier.setPrix(150.0);
            //lieuAModifier.setRegion("Nouvelle région");

            // Appelez la méthode modifier
            //ps.modifier(lieuAModifier);

            // Affichez un message de confirmation
            //System.out.println("Lieu modifié avec succès !");
            //System.out.println(ps.afficher());

        //} else {
            //System.out.println("Le lieu avec l'identifiant spécifié n'existe pas.");
        //}
        Lieu lieuASupprimer = ps.obtenirLieuParId(13);

        // Vérifier si le lieu existe avant de le supprimer
        if (lieuASupprimer != null) {
            // Supprimer le lieu
            ps.supprimer(lieuASupprimer);
            System.out.println("Lieu supprimé avec succès !");
        } else {
            System.out.println("Le lieu avec l'identifiant spécifié n'existe pas.");
        }


    }*/




    }}
